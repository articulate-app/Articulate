import type { ReactNode } from 'react'

/** Standalone documents chrome removed — route redirects into Settings → Teams. */
export default function DocumentsLayout({ children }: { children: ReactNode }) {
  return children
}
