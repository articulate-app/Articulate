import type { ReactNode } from 'react'

/** Standalone financials chrome removed — route redirects into Settings → Teams. */
export default function FinancialsLayout({ children }: { children: ReactNode }) {
  return children
}
